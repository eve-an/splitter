---
# Base packages installed on every host. Extend per-host if required.
system_base_packages:
  - ufw
  - fail2ban
  - unattended-upgrades
  - htop
  - vim
  - rsync
  - python3
  - python3-pip
  - python3-apt
  - git
  - acl

# Additional Debian repositories to enable. Example structure:
# system_extra_apt_repos:
#   - repo: "deb http://deb.debian.org/debian bookworm-backports main"
#     file: bookworm-backports.list
system_extra_apt_repos: []
